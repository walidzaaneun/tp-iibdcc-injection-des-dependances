# Activité Pratique N°1 - Injection des dépendances (Parite 1)


### Objectif :
démonstrer le couplage faible et l’injection de dépendances en Java.

### Méthodes d’injection
1. Statique (instanciation directe)
2. Dynamique (Reflection + `config.txt`)
3. Spring XML (`config.xml`)
4. Spring Annotations (`@Component`, `@Autowired`)

### Exécution
Lancer `Main` pour tester :
```
calcul = ...
```